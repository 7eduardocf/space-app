import styled from "styled-components";
import BarraLateral from "../sidebar/barra-lateral";
import Banner from "../banner/banner";

const ContainerEstilizado = styled.section`
    display:flex;
    gap: 48px;
`
const Container = ()=>{
    return (
        <ContainerEstilizado>

        </ContainerEstilizado>
    )
}
export default Container